# Programa para medir la temperatura

def main():
    # Definimos una función llamada 'main'
    mensaje = "¡Hola, este es un programa en Python!"
    # Creamos una variable llamada 'mensaje' que contiene el texto del mensaje
    print(mensaje)
    # Usamos la función 'print' para mostrar el mensaje por pantalla

# Llamamos a la función 'main' para ejecutar el programa
if __name__ == "__main__":
    main()
